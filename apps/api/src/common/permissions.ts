/** Compatibility exports for modules that still import the old API path. */
export {
  BUILT_IN_ROLE_PERMISSIONS,
  PERMISSION_CATALOG,
  normalizePermissions,
  type PermissionKey,
} from '@betterspend/shared';
